let value =prompt('nhap value', 0);
 switch (value) {
    case (value > 0) :
        alert(1);
        break;
    case (value < 0) :
        alert(-1);
        break;
    default:  
        alert(0);
 }